import React from "react";
import './Loader.scss'

const Loader = props => {
    return (
        <div className="loader-container">
            <div  className="loader"/>
        </div>
    );
};

export default Loader;
